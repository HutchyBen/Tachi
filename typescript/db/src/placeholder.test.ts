// vitest requires that there is at least one test file in a project.
import { assert, describe, it } from "vitest";

describe("placeholder", () => {
	it("should placehold", () => {
		assert(true);
	});
});
